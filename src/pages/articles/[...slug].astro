---
import { getCollection, type CollectionEntry } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import CTASection from '../../components/CTASection.astro';
import MosaicDivider from '../../components/MosaicDivider.astro';
import PostCard from '../../components/PostCard.astro';
import { site, url } from '../../site.config';

export async function getStaticPaths() {
  const posts = await getCollection('posts', ({ data }) => !data.draft);
  return posts.map((post) => ({
    params: { slug: post.slug },
    props: { post },
  }));
}

interface Props {
  post: CollectionEntry<'posts'>;
}
const { post } = Astro.props;
const { Content } = await post.render();
const { title, description, category, heroImage, heroImageAlt, publishDate, tags, readingTime } =
  post.data;

// פוסטים קשורים — מאותה קטגוריה, אחרת אחרונים
const all = (await getCollection('posts', ({ data }) => !data.draft)).filter(
  (p) => p.slug !== post.slug
);
const related = [
  ...all.filter((p) => p.data.category === category),
  ...all.filter((p) => p.data.category !== category),
]
  .slice(0, 3);

const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'Article',
  headline: title,
  description,
  image: new URL(url(heroImage), Astro.site).href,
  datePublished: publishDate.toISOString(),
  articleSection: category,
  author: { '@type': 'Person', name: site.guideName },
  publisher: { '@type': 'Organization', name: site.name },
  mainEntityOfPage: Astro.url.href,
};
---

<BaseLayout
  title={title}
  description={description}
  image={heroImage}
  type="article"
  publishDate={publishDate}
  jsonLd={jsonLd}
>
  <!-- כותרת עם תמונת רקע -->
  <header class="relative isolate overflow-hidden">
    <img src={url(heroImage)} alt={heroImageAlt} class="absolute inset-0 -z-10 h-full w-full object-cover" fetchpriority="high" />
    <div
      class="absolute inset-0 -z-10"
      style="background: linear-gradient(180deg, rgba(27,57,68,0.12) 0%, rgba(27,57,68,0.18) 38%, rgba(27,57,68,0.60) 72%, rgba(27,57,68,0.93) 100%);"
      aria-hidden="true"
    >
    </div>
    <div class="container-prose flex min-h-[52vh] flex-col justify-end pb-12 pt-28 text-sand-50" style="text-shadow: 0 2px 16px rgba(11,26,32,0.6);">
      <a href={url('/articles')} class="mb-4 inline-flex w-fit items-center gap-1.5 text-sm text-sand-100/90 hover:text-gold-400">
        חזרה לכל המאמרים
      </a>
      <span class="mb-3 inline-flex w-fit rounded-full bg-sand-50/15 px-3 py-1 text-xs font-medium backdrop-blur-sm" style="text-shadow: none;">
        {category}
      </span>
      <h1 class="font-serif text-4xl leading-tight text-sand-50 sm:text-5xl md:text-6xl">
        {title}
      </h1>
      <div class="mt-5 flex flex-wrap items-center gap-x-4 gap-y-1 text-sm text-sand-100/90">
        <span>מאת {site.guideName}</span>
        {readingTime && <><span aria-hidden="true">·</span><span>{readingTime} דק׳ קריאה</span></>}
      </div>
    </div>
  </header>

  <!-- גוף המאמר -->
  <article class="container-prose py-14">
    <div class="prose-body">
      <Content />
    </div>

    {
      tags.length > 0 && (
        <div class="mt-12 flex flex-wrap gap-2 border-t border-sand-300 pt-6">
          {tags.map((tag) => (
            <span class="rounded-full bg-sand-200 px-3 py-1 text-xs text-ink-700">#{tag}</span>
          ))}
        </div>
      )
    }
  </article>

  <CTASection variant="inline" />

  <!-- פוסטים קשורים -->
  {
    related.length > 0 && (
      <section class="container-page py-14">
        <MosaicDivider class="mb-8" />
        <h2 class="rule-gold mb-8 font-serif text-2xl text-ink-900">להמשך הקריאה</h2>
        <div class="grid gap-7 sm:grid-cols-2 lg:grid-cols-3">
          {related.map((p) => <PostCard post={p} />)}
        </div>
      </section>
    )
  }
</BaseLayout>
